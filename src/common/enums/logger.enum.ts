export enum LoggerCtx {
  HTTP = 'HTTP',
  SERVICE = 'Service',
  SQL = 'SQL',
  VALIDATION_PIPE = 'ValidationPipe',
}

export enum LogLevel {
  DEBUG = 'debug',
  ERROR = 'error',
  INFO = 'info',
  TRACE = 'trace',
  WARN = 'warn',
}
